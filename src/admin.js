import { supabase } from "./supabase.js";
const css=document.createElement("link");css.rel="stylesheet";css.href="/src/admin.css";document.head.appendChild(css);
const app=document.querySelector("#app");const state={user:null,profile:null,users:[]};
const esc=(v="")=>String(v).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;");
const money=v=>`$${Number(v||0).toFixed(2)}`;const num=v=>Number(v||0).toLocaleString("en-US");const ctr=u=>u?.impressions?`${((Number(u.clicks||0)/Number(u.impressions||0))*100).toFixed(2)}%`:"0.00%";
function toast(msg){const el=document.querySelector("#toast");if(!el)return;el.textContent=msg;el.classList.add("show");clearTimeout(window.__t);window.__t=setTimeout(()=>el.classList.remove("show"),2400)}
function landingUrl(id){return `${location.origin}/member.html?id=${encodeURIComponent(id)}`}
function copyLandingUrl(id){const url=landingUrl(id);navigator.clipboard?.writeText(url).then(()=>toast("Landing page URL copied.")).catch(()=>{window.prompt("Copy landing page URL:",url)})}
async function verifyAdmin(){const {data:{user}}=await supabase.auth.getUser();if(!user)throw new Error("Not logged in.");const {data:profile,error}=await supabase.from("profiles").select("*").eq("id",user.id).single();if(error)throw error;if(profile.role!=="admin")throw new Error("Akun ini bukan admin.");state.user=user;state.profile=profile}
async function loadData(){const {data:profiles,error}=await supabase.from("profiles").select("*").order("created_at",{ascending:false});if(error)throw error;state.users=profiles||[]}
function render(){const totalRevenue=state.users.reduce((s,u)=>s+Number(u.revenue||0),0),totalImpressions=state.users.reduce((s,u)=>s+Number(u.impressions||0),0),totalClicks=state.users.reduce((s,u)=>s+Number(u.clicks||0),0);app.innerHTML=`<main class="admin-page"><header class="topbar"><div><div class="eyebrow">ADMIN PANEL</div><h1>User Management</h1></div><button id="logout" class="ghost">LOG OUT</button></header><section class="summary"><div><span>USERS</span><strong>${num(state.users.length)}</strong></div><div><span>IMPRESSIONS</span><strong>${num(totalImpressions)}</strong></div><div><span>CLICKS</span><strong>${num(totalClicks)}</strong></div><div><span>REVENUE</span><strong>${money(totalRevenue)}</strong></div></section><section class="panel"><div class="panel-head"><h2>Users</h2><div style="display:flex;gap:8px;align-items:center"><input id="userSearch" placeholder="Search name or email"><button id="newUser" class="primary">+ ADD USER</button></div></div><div class="table-wrap"><table><thead><tr><th>Name / Email</th><th>Membership</th><th>Status</th><th>Role</th><th>Offer URL</th><th>Banner</th><th>Landing Page</th><th>Impressions</th><th>Clicks</th><th>CTR</th><th>CPM</th><th>Revenue</th><th>Unlock / 60m</th><th>IP / 24h</th><th>Actions</th></tr></thead><tbody>${state.users.map(u=>`<tr><td><strong>${esc(u.name)}</strong><small>${esc(u.email||"")}</small></td><td>${esc(u.membership)}</td><td><span class="badge">${esc(u.account_status)}</span></td><td>${esc(u.role)}</td><td>${u.offer_url?`<a href="${esc(u.offer_url)}" target="_blank" rel="noopener noreferrer">OPEN</a>`:"—"}</td><td>${u.banner_code?"SET":"—"}</td><td><div style="display:flex;gap:5px;align-items:center;flex-wrap:wrap"><a href="${landingUrl(u.id)}" target="_blank" rel="noopener noreferrer">OPEN</a><button class="copy-landing" data-id="${u.id}" type="button">COPY</button></div></td><td>${num(u.impressions)}</td><td>${num(u.clicks)}</td><td>${ctr(u)}</td><td>${money(u.cpm)}</td><td>${money(u.revenue)}</td><td>${num(u.unlock_limit)}</td><td>${num(u.ip_offer_limit||1)}</td><td><button class="edit-user" data-id="${u.id}">EDIT</button></td></tr>`).join("")}</tbody></table></div></section></main><div id="toast" class="toast"></div>`;document.querySelector("#logout").onclick=async()=>{await supabase.auth.signOut();location.reload()};document.querySelector("#newUser").onclick=()=>createUserForm();document.querySelectorAll(".edit-user").forEach(btn=>btn.onclick=()=>userForm(state.users.find(u=>u.id===btn.dataset.id)));document.querySelectorAll(".copy-landing").forEach(btn=>btn.onclick=()=>copyLandingUrl(btn.dataset.id));document.querySelector("#userSearch").oninput=e=>{const q=e.target.value.toLowerCase();document.querySelectorAll("tbody tr").forEach(row=>row.style.display=row.textContent.toLowerCase().includes(q)?"":"none")}}
function createUserForm(){app.innerHTML+=`<div class="modal" id="modal"><form class="modal-card" id="createUserForm"><h2>Create New User</h2><label>Name</label><input id="newName" placeholder="Full name" required><label>Email</label><input id="newEmail" type="email" placeholder="user@example.com" required><label>Password</label><input id="newPassword" type="password" minlength="8" placeholder="Minimum 8 characters" required><label>Membership</label><select id="newMembership"><option>Standard</option><option>Premium</option><option>VIP</option></select><label>Account Status</label><select id="newStatus"><option>Verified</option><option>Pending</option><option>Suspended</option></select><label>Role</label><select id="newRole"><option value="user">user</option><option value="admin">admin</option></select><label>Offer Name</label><input id="newCampaignName" placeholder="Member Offer" value="Member Offer"><label>Offer URL</label><input id="newOfferUrl" type="url" placeholder="https://example.com/offer"><label>Banner Code</label><textarea id="newBannerCode" rows="7" placeholder="Tempel kode banner HTML/JavaScript untuk offer ini"></textarea><label>Impressions</label><input id="newImp" type="number" min="0" value="0"><label>Clicks</label><input id="newClicks" type="number" min="0" value="0"><label>CPM</label><input id="newCpm" type="number" min="0" step="0.01" value="0"><label>Revenue</label><input id="newRevenue" type="number" min="0" step="0.01" value="0"><label>Unlock limit per 60 minutes</label><input id="newUnlockLimit" type="number" min="0" value="3"><label>IP access limit per 24 hours</label><input id="newIpLimit" type="number" min="1" value="1"><div class="modal-actions"><button type="button" id="cancel">CANCEL</button><button class="primary" type="submit">CREATE USER</button></div></form></div>`;document.querySelector("#cancel").onclick=()=>document.querySelector("#modal").remove();document.querySelector("#createUserForm").onsubmit=async e=>{e.preventDefault();const offerUrl=document.querySelector("#newOfferUrl").value.trim();if(offerUrl){try{const parsed=new URL(offerUrl);if(!/^https?:$/.test(parsed.protocol))throw new Error()}catch{return toast("Offer URL harus berupa http:// atau https:// URL yang valid.")}}const {data,error}=await supabase.functions.invoke("admin-create-user",{body:{name:document.querySelector("#newName").value.trim(),email:document.querySelector("#newEmail").value.trim(),password:document.querySelector("#newPassword").value,membership:document.querySelector("#newMembership").value,account_status:document.querySelector("#newStatus").value,role:document.querySelector("#newRole").value}});if(error)return toast(error.message);if(!data?.ok)return toast(data?.error||"Could not create user.");const userId=data.user_id;const payload={campaign_name:document.querySelector("#newCampaignName").value.trim()||"Member Offer",offer_url:offerUrl||null,banner_code:document.querySelector("#newBannerCode").value,impressions:Number(document.querySelector("#newImp").value||0),clicks:Number(document.querySelector("#newClicks").value||0),cpm:Number(document.querySelector("#newCpm").value||0),revenue:Number(document.querySelector("#newRevenue").value||0),unlock_limit:Number(document.querySelector("#newUnlockLimit").value||0),ip_offer_limit:Number(document.querySelector("#newIpLimit").value||1)};const {error:updateError}=await supabase.from("profiles").update(payload).eq("id",userId);if(updateError)return toast(updateError.message);document.querySelector("#modal").remove();await loadData();render();toast("User created successfully. Landing page otomatis tersedia.")}}
function userForm(u){app.innerHTML+=`<div class="modal" id="modal"><form class="modal-card" id="userForm"><h2>Edit User & Offer</h2><label>Name</label><input id="uName" value="${esc(u.name)}" required><label>Membership</label><select id="uMembership">${["Standard","Premium","VIP"].map(x=>`<option ${u.membership===x?"selected":""}>${x}</option>`).join("")}</select><label>Status</label><select id="uStatus">${["Verified","Pending","Suspended"].map(x=>`<option ${u.account_status===x?"selected":""}>${x}</option>`).join("")}</select><label>Role</label><select id="uRole">${["user","admin"].map(x=>`<option ${u.role===x?"selected":""}>${x}</option>`).join("")}</select><label>Offer Name</label><input id="uCampaignName" value="${esc(u.campaign_name||"Member Offer")}" required><label>Offer URL</label><input id="uOfferUrl" type="url" value="${esc(u.offer_url||"")}" placeholder="https://example.com/offer"><label>Banner Code</label><textarea id="uBannerCode" rows="9" placeholder="Tempel kode banner HTML/JavaScript untuk offer ini">${esc(u.banner_code||"")}</textarea><label>Password Baru <span style="font-weight:normal;color:#64748b">(opsional)</span></label><input id="uPassword" type="password" minlength="8" autocomplete="new-password" placeholder="Kosongkan jika tidak ingin mengganti password"><small style="display:block;margin-top:-4px;margin-bottom:10px;color:#64748b">Password hanya dikirim ke Supabase dan tidak ditampilkan kembali.</small><label>Impressions</label><input id="uImp" type="number" min="0" value="${u.impressions??0}" required><label>Clicks</label><input id="uClicks" type="number" min="0" value="${u.clicks??0}" required><label>CPM</label><input id="uCpm" type="number" min="0" step="0.01" value="${u.cpm??0}" required><label>Revenue</label><input id="uRevenue" type="number" min="0" step="0.01" value="${u.revenue??0}" required><label>Unlock limit per 60 minutes</label><input id="uUnlockLimit" type="number" min="0" value="${u.unlock_limit??3}" required><label>IP access limit per 24 hours</label><input id="uIpLimit" type="number" min="1" value="${u.ip_offer_limit??1}" required><div class="modal-actions"><button type="button" id="cancel">CANCEL</button><button class="primary" type="submit">SAVE</button></div></form></div>`;document.querySelector("#cancel").onclick=()=>document.querySelector("#modal").remove();document.querySelector("#userForm").onsubmit=async e=>{e.preventDefault();const offerUrl=document.querySelector("#uOfferUrl").value.trim();if(offerUrl){try{const parsed=new URL(offerUrl);if(!/^https?:$/.test(parsed.protocol))throw new Error()}catch{return toast("Offer URL harus berupa http:// atau https:// URL yang valid.")}}const password=document.querySelector("#uPassword").value;const payload={name:document.querySelector("#uName").value.trim(),membership:document.querySelector("#uMembership").value,account_status:document.querySelector("#uStatus").value,role:document.querySelector("#uRole").value,campaign_name:document.querySelector("#uCampaignName").value.trim()||"Member Offer",offer_url:offerUrl||null,banner_code:document.querySelector("#uBannerCode").value,impressions:Number(document.querySelector("#uImp").value||0),clicks:Number(document.querySelector("#uClicks").value||0),cpm:Number(document.querySelector("#uCpm").value||0),revenue:Number(document.querySelector("#uRevenue").value||0),unlock_limit:Number(document.querySelector("#uUnlockLimit").value||0),ip_offer_limit:Number(document.querySelector("#uIpLimit").value||1)};const {error}=await supabase.from("profiles").update(payload).eq("id",u.id);if(error)return toast(error.message);if(password){if(u.id===state.user.id)return toast("Untuk keamanan, password admin diganti melalui pengaturan admin.");const {data:resetData,error:resetError}=await supabase.functions.invoke("admin-reset-password",{body:{user_id:u.id,password}});if(resetError)return toast(resetError.message);if(!resetData?.ok)return toast(resetData?.error||"Password gagal diubah.")}document.querySelector("#modal").remove();await loadData();render();toast(password?"User updated. Password berhasil diubah.":"User updated.")}}
async function start(){try{await verifyAdmin();await loadData();render()}catch(e){app.innerHTML=`<main class="login-shell"><section class="login-card"><h1>Admin Access</h1><p>${esc(e.message)}</p><button class="primary" id="back">BACK TO LOGIN</button></section></main>`;document.querySelector("#back").onclick=()=>location.href="/"}}
start();
